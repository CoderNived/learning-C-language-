// Write a C program that calculates the volume of a sphere. //
#include<stdio.h>
int main()
{
    int r;
    float volume;
    printf("enter the radius of the sphere: ");
    scanf("%d",&r);
    volume = (4.0/3.0)*3.14*r*r*r;
    printf("the volume of the given sphere is %f\n",volume);
}
